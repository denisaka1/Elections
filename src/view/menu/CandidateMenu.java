package view.menu;

import javafx.stage.Stage;
import view.InputMenu;

public class CandidateMenu extends InputMenu {
    public CandidateMenu(Stage stage) {
        super(stage);
        menu.setTitle("Candidate Menu");

        // TODO: add fields: ID, Party Name, Place
    }
}
